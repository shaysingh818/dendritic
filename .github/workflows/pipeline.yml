name: Dendritic

on:
  pull_request:
    branches: [ "master" ]

jobs:

  ndarray:
    runs-on: ubuntu-20.04
    steps:
    - uses: actions/checkout@v2

    - name: Build
      working-directory: "./crates"
      run: cargo build -p dendritic-ndarray --release --color always

    - name: Test
      working-directory: "./crates"
      run: cargo test dendritic-ndarray --release --color always

  metrics:
    runs-on: ubuntu-20.04
    needs: [ndarray]
    steps:
    - uses: actions/checkout@v2

    - name: Build
      working-directory: "./crates/dendritic-metrics"
      run: cargo build --release --color always

    - name: Test
      working-directory: "./crates/dendritic-metrics"
      run: cargo test --release --color always

  autodiff:
    runs-on: ubuntu-20.04
    needs: [ndarray]
    steps:
    - uses: actions/checkout@v2

    - name: Build
      working-directory: "./crates/dendritic-autodiff"
      run: cargo build --release --color always

    - name: Test
      working-directory: "./crates/dendritic-autodiff"
      run: cargo test --release --color always

  preprocessing:
    runs-on: ubuntu-20.04
    needs: [ndarray]
    steps:
    - uses: actions/checkout@v2

    - name: Build
      working-directory: "./crates/dendritic-preprocessing"
      run: cargo build --release --color always

    - name: Test
      working-directory: "./crates/dendritic-preprocessing"
      run: cargo test --release --color always

  datasets:
    runs-on: ubuntu-20.04
    needs: [ndarray]
    steps:
    - uses: actions/checkout@v2

    - name: Build
      working-directory: "./crates/dendritic-datasets"
      run: cargo build --release --color always

    - name: Test
      working-directory: "./crates/dendritic-datasets"
      run: cargo test --release --color always

  trees:
    runs-on: ubuntu-20.04
    needs: [ndarray, metrics, autodiff, preprocessing, datasets]
    steps:
    - uses: actions/checkout@v2

    - name: Build
      working-directory: "./crates/dendritic-trees"
      run: cargo build --release --color always

    - name: Test
      working-directory: "./crates/dendritic-trees"
      run: cargo test --release --color always

  knn:
    runs-on: ubuntu-20.04
    needs: [ndarray, metrics, autodiff, preprocessing, datasets]
    steps:
    - uses: actions/checkout@v2

    - name: Build
      working-directory: "./crates/dendritic-knn"
      run: cargo build --release --color always

    - name: Test
      working-directory: "./crates/dendritic-knn"
      run: cargo test --release --color always


  regression:
    runs-on: ubuntu-20.04
    needs: [ndarray, metrics, autodiff, preprocessing, datasets]
    steps:
    - uses: actions/checkout@v2

    - name: Build
      working-directory: "./crates/dendritic-regression"
      run: cargo build --release --color always

    - name: Test
      working-directory: "./crates/dendritic-regression"
      run: cargo test --release --color always

  bayes:
    runs-on: ubuntu-20.04
    needs: [ndarray, metrics, autodiff, preprocessing, datasets]
    steps:
    - uses: actions/checkout@v2

    - name: Build
      working-directory: "./crates/dendritic-bayes"
      run: cargo build --release --color always

    - name: Test
      working-directory: "./crates/dendritic-bayes"
      run: cargo test --release --color always

  clustering:
    runs-on: ubuntu-20.04
    needs: [ndarray, metrics, autodiff, preprocessing, datasets]
    steps:
    - uses: actions/checkout@v2

    - name: Build
      working-directory: "./crates/dendritic-clustering"
      run: cargo build --release --color always

    - name: Test
      working-directory: "./crates/dendritic-clustering"
      run: cargo test --release --color always


